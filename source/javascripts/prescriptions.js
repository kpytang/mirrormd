/*
// toggle visibility of form
$("#show-form-button").click(function() {
	$("#prescription-form").toggle("normal");
});
// form tooltip
$(function() {
    $( document ).tooltip();
});
// append form data to table- how do I select inputs separately and in order to append?
$('id="submit-form-button').on('submit', function (e) {
	e.preventDefault();
	var newName = $('form').find('input[type="text"]').val();
	$('table').append('<tr><td>'  newName  '</td></tr>')
});
*/
